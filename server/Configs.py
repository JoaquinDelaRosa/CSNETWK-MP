# To connect anywhere in the private network using all available private interfaces, set IP_ADDRESS to "0.0.0.0"
IP_ADDRESS = "0.0.0.0"                  

PORT_NUMBER = 3000


ALLOW_BLOCKING_PROTOCOL = True          # Disable this if blocking/unblocking is to be removed.